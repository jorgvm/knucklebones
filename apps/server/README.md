# Render.com

You can run a free web service on render.com

Basic setup:

- root directory: `apps/server`
- build command: `pnpm install --frozen-lockfile`
- start command: `pnpm start`
- add variables, see .env.example
